---
- name: posformatacaoCentos
  hosts: all 

  tasks:
  - name: prepara repositorio
    shell: files/configurarRepositorio.sh >> somelog.txt
    
  - name: posformatacaoCentos
    apt:
      name: "{{ packages }}"
    vars:
      packages:
        - python-pip
        - git
        - docker-ce
        - java-1.8.0-openjdk-devel
        - java-1.8.0-openjdk 
        - wget
        - VirtualBox-5.2
        - google-chrome-stable
        
    reboot:
